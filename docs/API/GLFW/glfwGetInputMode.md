# glfwGetInputMode
glfwGetInputMode

```php
function glfwGetInputMode(\GLFWwindow $window, int $mode) : int
```

arguments

:    1. `\GLFWwindow` `$window` 
    2. `int` `$mode` 

returns

:    `int` 

---
     